"""Константы и справочники предметной области.

Что здесь лежит:
    tariffs.py — справочник тарифов: комбо против обычных, ГБ обхода,
                 названия для интерфейса, доступные периоды.
    loyalty.py — уровни программы лояльности.

Модули импортируются явно (`from app.constants import tariffs`), поэтому
пакет ничего не реэкспортирует: так видно, откуда взялось имя.
"""
