/**
 * СОВМЕСТИМОСТЬ. Реализация переехала в `@/components/ui/StatCard` — там же,
 * где остальные примитивы, и там же описано, почему из карточки ушли градиент
 * и иконка.
 *
 * Этот файл оставлен реэкспортом, потому что на путь `@/components/StatCard`
 * ссылаются страницы вне текущей переделки (Referrals, BypassGifts). Удалять
 * его можно будет только вместе с правкой их импортов — иначе упадёт сборка.
 */
export { StatCard, type StatCardProps } from "./ui/StatCard";
