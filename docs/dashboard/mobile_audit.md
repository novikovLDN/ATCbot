# Дашборд: аудит на всех устройствах и iOS (2026-09-13)

Ветка — рабочая ветка worktree поверх `refactor/audit-2026-09`. Код: `dashboard/`,
`app/api/dashboard/`. Проверка — WebKit (движок Safari), Playwright-эмуляция iPhone/iPad
с touch, dev-сервер Vite с моком `VITE_MOCK_API=1` (`dashboard/mock-api.ts`, только
`vite serve`, в прод-сборку не попадает).

Ширины: 360 (Android), 375 (iPhone SE 2/3), 393 (iPhone 15/16), 430 (Pro Max),
768 (iPad портрет), 1024 (iPad альбом), 1440 (десктоп). Все 23 экрана × 7 ширин, плюс
светлая тема (375/1440), плюс эмуляция встроенного браузера Telegram iOS. Автопроверка на
каждом снимке: горизонтальный скролл страницы, элементы за краем, поля ввода < 16px,
ошибки в консоли.

Скриншоты (вне репо): `…/scratchpad/dashboard-shots/{before,after,after-light,after-webview}/`,
имя файла `<экран>__<ширина>.png`. Скрипты съёмки там же: `shoot.mjs`, `probe-more.mjs`,
`probe-modal.mjs`.

## Что было сломано и что исправлено

| Экран × устройство | Было | Стало |
|---|---|---|
| Все экраны, телефоны (360–430) | Меню «Ещё» открывалось внутри горизонтально скроллящейся строки вкладок и обрезалось до одного пункта («Рассылки»). 13 разделов были недоступны с телефона | Ниже md меню открывается как bottom sheet с затемнением над home indicator. Все 13 пунктов видны и прокручиваются (`before/more-menu__375-se.png` → `after/more-menu__375-se.png`) |
| Все экраны, touch | Вкладки 36px, кнопки шапки 40px | 44px на `pointer: coarse` (Apple HIG) |
| Все формы, iPhone | Поля 14px (`text-sm`): Safari зумил страницу при фокусе (поиск пользователей, промокоды, рассылки, сервис, карточка пользователя — до 8 полей) | На touch-экранах все поля 16px; зума нет |
| Модалки (промокод, гифт-ГБ, расписание рассылки, автоуведомления), телефон | Оверлей `fixed` без прокрутки, карточка по центру: на коротком экране или с открытой клавиатурой верх и низ обрезаны, кнопки недостижимы. У автоуведомлений `max-h-[90vh]` (на iOS vh — «большой» вьюпорт) | Оверлей скроллится, карточка прижата к верху, отступы от выреза и home indicator; на 375×560 карточка целиком достижима (`after/modal-promo__375x560.png`, `modal-bgift__375x560.png`) |
| Тосты, iPhone в режиме приложения | `top-4` — под часами и вырезом | `max(1rem, safe-area-inset-top)`, не перехватывают тапы по странице |
| Подсказка «На экран Домой», iPhone | Висела на 88px от низа (место под несуществующий bottom bar) поверх контента | Над home indicator |
| Вход / первая настройка, iOS-приложение | Нет safe-area отступов: карточка под статус-баром | Отступы safe-area сверху и снизу |
| Пользователи, 360 | Переключатель плотности вылезал за край (scrollWidth 372/360) | Шапка переносится (`before/users__360-android.png` → `after/…`) |
| Сервис, 360 | Кнопка «Включить» (режим инцидента) за краем (366/360) | Шапка карточки переносится |
| Панель, все | Трафик: «2.1 TiB» (строка панели) рядом с нашим «412,0 ТБ»; подпись «Трафик по нодам» сжималась в три строки рядом с переключателем | Все строки через `fmtBytes` («2,1 ТБ»); строка подписи тайла переносится (`after/panel__375-se.png`) |
| Статистика, все | Белый экран: `useMemo` после ранних `return` в `SegmentsMini` → «Rendered more hooks» после загрузки сегментов | Хуки до `return`; экран рисуется (0 ошибок) |
| Любой экран | Одна ошибка рендера размонтировала всё приложение (пустая страница без шапки и навигации; в iOS-приложении нет даже кнопок браузера) | Error boundary на уровне экрана: шапка и навигация живы, кнопка «Обновить», сброс при переходе |
| Рассылки / Операции | `GET /broadcasts/scheduled` → 422 (роут объявлен после `/{broadcast_id}`), список запланированных не грузился | `/scheduled*` объявлены раньше, тест `tests/api/test_dashboard_route_contracts.py` |
| Аудит трафика → «Ресинк» | `panel_used_bytes` читался с верхнего уровня (в Remnawave 3.4.3 его нет) → всегда 0 | `panel_traffic_audit.used_traffic_bytes` (userTraffic, fallback наверх), тест `tests/api/test_dashboard_traffic_audit.py` |
| Сброс premium-лимитов (dry-run) | При пустой выборке ответ без `dry_run`/`samples` → UI писал «Сброшено: 0» вместо dry-run | Поля всегда есть |
| Даты | `fmtDate` в часовом поясе устройства, а дни/графики/расписания — по Москве | `timeZone: Europe/Moscow` |
| Настройки, Статистика, платежи пользователя | Описание алерта «Platega / CryptoBot / Lava»; у WATA не было подписи (показывалось `wata`) | «WATA» в тексте и словарях; Lava оставлена только для исторических строк |
| PWA-заставка, первый кадр | `theme-color`/manifest `#1E1E1E` (уголь) при светло-серой «стене» основного вида: вспышка угля при запуске | `#CFCECA` в index.html и manifest; `apple-mobile-web-app-title`; `format-detection=telephone=no` (Safari делал из Telegram ID телефонные ссылки) |

Раскладка на 768/1024/1440 проблем не показала: bento-сетка 1 → 6 → 12 колонок, таблицы и
графики скроллятся внутри своих контейнеров, горизонтального скролла страницы нет ни на
одной ширине. Светлая тема на 375/1440 без дефектов (`after-light/`).

## iOS: Safari, приложение на экране «Домой», встроенный браузер Telegram

- **Safari**: вход паролем и passkey работают. Cookie `atlas_admin_session` — HttpOnly,
  Secure, SameSite=Lax, same-origin. Magic-link (15 мин) — только для первой настройки
  (`POST /auth/setup`, токен в теле). Защиты не менялись: bootstrap-only magic link,
  rate limit, CSRF по Origin, WS только по cookie, Idempotency-Key.
- **Приложение на экране «Домой» (standalone)**: `viewport-fit=cover`, safe-area отступы у
  шапки, тостов, меню, модалок, экрана входа. `100svh` / `100dvh` вместо `100vh`. Кнопок
  браузера нет, но каждый раздел доступен из шапки или меню «Ещё», у вложенных экранов
  (карточка пользователя, новая рассылка) есть «К списку» / «Назад», а ошибка экрана больше
  не оставляет пустую страницу. Хранилище standalone-приложения на iOS отдельное от Safari:
  войти нужно в самом приложении. Passkey из iCloud Keychain там работает.
- **Встроенный браузер Telegram iOS** (WKWebView): `PublicKeyCredential` там есть, но iOS
  не показывает окно passkey чужим WebView без associated-domains — кнопка Face ID
  молча не работала. Теперь такой браузер определяется (`isInAppBrowser`: нет токена
  `Safari/`, `TelegramWebviewProxy`, UA Telegram/Instagram/VK/FB). Кнопку Face ID
  заменяет пояснение «⋯ → Открыть в Safari» и кнопка «Скопировать адрес дашборда»
  (обычный адрес, не magic-link). Вход паролем и первая настройка по ссылке из `/admin`
  там работают (`after-webview/login__393-15.png`).

**Невозможно исправить со стороны веба:**
- passkey во встроенном браузере Telegram — ограничение iOS. Программно открыть Safari из
  WKWebView нельзя, поэтому только инструкция.
- Сессия не переносится между Safari, приложением на экране «Домой» и WebView Telegram —
  у каждого своё хранилище cookie.
- Цвет статус-бара в iOS-приложении читается один раз при запуске, из
  `apple-mobile-web-app-status-bar-style`. Сейчас это `black-translucent` (контент под
  статус-баром, отступы safe-area есть). В основном виде светлая «стена» даёт белые часы
  на светло-сером фоне — контраст низкий. Переключение темы в приложении это не меняет.
  Вариант `default` дал бы белую полосу над тёмной стеной в светлой теме. Оставлено как
  есть, решение за владельцем.
- Web Push на iOS — только в установленном приложении (iOS 16.4+).

## Функциональные баги

Исправлены: 422 списка запланированных рассылок; `panel_used_bytes` = 0 в ресинке аудита
трафика; dry-run сброса premium-лимитов; белый экран «Статистики»; падение всего приложения
от ошибки одного экрана; смешанные единицы трафика; даты не по Москве; остатки Lava и нет
подписи WATA. Мок разработки теперь отдаёт бренд Atlas Secure / atlassecure_bot, WATA и
правильные формы ответов (раньше `/broadcasts/scheduled`, `/automated-notifications/`,
`/pricing/tariffs`, `/payments/breakdown` получали `{}`/`[]` и валили экраны при ревью —
на реальном бэкенде эти формы верные).

Найдены в этом аудите и **исправлены в dashboard v4 (2026-09-14)**:
- ~~Старые экраны на типографике v2 внутри общего листа~~ — все старые экраны (Рассылки,
  Новая рассылка, Статистика, Рефералы, Промокоды, Ссылки, Аудит, Bypass-аудит, Аудит
  трафика, Сервис, Автоуведомления, Гифт-ГБ, Цены, Пользователи и карточка пользователя,
  Настройки, VPN-Инноватор) переведены на bento v3: тайлы вместо рамок, капсулы вместо
  uppercase-подписей, одна шкала шрифтов, только токены цвета (тёмная и светлая тема).
  Обёртка `Sheet` удалена, 7 неиспользуемых старых компонентов удалены. Поведение, запросы и
  ключи идемпотентности не менялись.
- ~~Иконки-кнопки ≈ 32px~~ — на touch-экранах у `.icon-btn-sm` и у мелких кнопок с классом
  `tap-target` невидимая зона нажатия 44×44, у `.btn` — `min-height: 44px`; «?»-подсказки
  тоже 44px. Закрытие тоста и подсказки «На экран Домой» — 44px.
- ~~24 неиспользуемые функции в `lib/api.ts`~~ — удалены 22 (grep-доказательство в коммите),
  `downloadCsv` подключён: «Экспорт CSV» на экранах «Пользователи» (`/export/users.csv`) и
  «Подписчики» (`/export/subscriptions.csv`).
- ~~Один бандл 1,06 МБ~~ — ленивые чанки на каждый экран: входной чанк 288,6 КБ (92,7 КБ gzip),
  recharts отдельно (408 КБ, грузится только экранами с графиками). Предупреждения Vite нет.

Найдено и исправлено при проверке v4: пустые столбики «Истекают по дням» в WebKit (проценты
высоты внутри flex-элемента → пиксели); горизонтальный скролл «Пользователей» на 375/393
(`sr-only`-подпись таблицы выходила из контейнера прокрутки).

Остаётся (не исправимо со стороны веба или решение владельца) — см. раздел iOS выше:
passkey во встроенном браузере Telegram, раздельные cookie Safari / приложения / WebView,
цвет статус-бара в iOS-приложении.

## Проверка v4 (2026-09-14)

- `npx tsc` (app и node) — 0 ошибок; `npm run build` — успешно, без предупреждения о размере.
- `pytest` — 2754 passed, 4 skipped, 139 xfailed (с `DASHBOARD_TEST_DATABASE_URL`: 16 SQL-тестов
  на PostgreSQL 16); `ruff check .` — чисто; `python -c "import tests.conftest, main"` — ок.
- WebKit-съёмка v4 (эмуляция iPhone SE / 15, iPad, десктоп): тёмная тема — 111 снимков (все
  23 экрана на 375/393/768/1024 и 19 экранов на 1440), светлая — 45 (375 и 1440), повтор
  исправленных «Обзора», «Подписчиков» и «Пользователей» на всех 5 ширинах в обеих темах — 30/30.
  После исправлений: 0 горизонтального скролла, 0 элементов за краем, 0 ошибок консоли.
  «Настройки» на 1440 в WebKit не сняты: Playwright зависает на шаге снимка в ожидании шрифтов
  (страница при этом отвечает); в Chromium экран рисуется за 1,3 с, на 375–1024 в WebKit — чисто.
  Скриншоты: `…/scratchpad/dashboard-v4/{dark,light}/<экран>__<ширина>.png`, скрипт `shoot.mjs`.

## Проверка

- `npx tsc -p tsconfig.app.json --noEmit` — 0 ошибок; `npx tsc -p tsconfig.node.json --noEmit` — 0.
- `npm run build` — успешно (предупреждение о размере чанка).
- `.venv/bin/python -m pytest -q` — 1580 passed, 4 skipped, 12 xfailed.
- `.venv/bin/ruff check .` — All checks passed.
- WebKit-съёмка: 23 экрана × 7 ширин — 0 горизонтального скролла, 0 ошибок консоли
  после исправлений.
