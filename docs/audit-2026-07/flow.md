# Флоу бота, экраны и кнопки

Находок: **24** — P0 0, P1 2, P2 19, P3 3. Опровергнуто при перепроверке: 1.

Перепроверялись только три самые тяжёлые находки домена. Остальные помечены «не перепроверено» — это гипотезы, требующие подтверждения перед правкой.

---

### 1. Главный экран для неруссскоязычных юзеров без подписки показывает сырой ключ вместо текста

**P1** · ✅ подтверждено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:116`

**Дефект.** _get_main_text запрашивает main.welcome_expired (строка 116) и main.welcome_no_sub (строка 118). Оба ключа есть только в ru: проверено загрузкой всех семи словарей — en/de/kk/ar/uz/tj их не содержат. get_text (app/i18n/__init__.py:44-65) пробует запрошенный язык, затем en, затем возвращает САМ КЛЮЧ. Русского фолбэка нет.

**Когда ломается.** Юзер выбрал English/Deutsch/O'zbek на экране выбора языка (он показывается на каждом /start, start.py:452), подписки нет. Жмёт любую кнопку «Назад» → menu_main → бот присылает фото, в подписи которого буквально строка «main.welcome_no_sub». Это главный конверсионный экран — юзер не видит ни оффера, ни описания сервиса.

**Что сделать.** Добавить main.welcome_no_sub и main.welcome_expired во все шесть неканонических словарей (или, как временная мера, ввести в get_text третий фолбэк на ru перед возвратом ключа). Заодно решить, что фолбэк на сырой ключ в пользовательских экранах недопустим.

**Уточнение проверки.** Находка верна, но занижает область поражения: сырой ключ показывается не только по кнопке «Назад» → menu_main. `_get_main_text` вызывается из пяти мест — navigation.py:63 (menu_main), :88 (back_to_main), :1200 (setup_done), app/handlers/user/connect.py:123 и, главное, app/handlers/callbacks/language.py:71-83, где главный экран рендерится СРАЗУ после нажатия кнопки языка. То есть новый пользователь, выбравший English/Deutsch/O'zbek/Қазақша/العربية/Тоҷикӣ на первом же /start, немедленно получает фото с подписью «main.welcome_no_sub» — до всяких «Назад». Дополнение: третий ключ той же функции, `main.welcome_bypass` (navigation.py:121), в en есть (app/i18n/en.py:496), поэтому для de/kk/ar/uz/tj он деградирует до английского, а не до сырого ключа — эта ветка не сломана. Ещё нюанс фолбэка: неизвестный код языка уходит на ru-словарь (app/i18n/__init__.py:46), так что ломаются ровно те шесть кодов, которые предлагает клавиатура выбора языка. Severity P1 оставляю: экран главный и конверсионный, дефект детерминированный. Единственный аргумент за P2 — доля неруссскоязычного трафика неизвестна (продовых логов и доступа к БД нет, см. BASELINE_FACTS п.3), поэтому масштаб влияния статикой не измеряется.

<details><summary>Как это проверялось</summary>

Опровергнуть не удалось — находка воспроизводится и в статике, и в рантайме.

1) Место существует и написано ровно то, что утверждается. `/Users/maximiliannovikov/Desktop/ATCbot-main/app/handlers/callbacks/navigation.py:104-126` — `_get_main_text`: при `not sub` берётся `main.welcome_expired` (строка 116) либо `main.welcome_no_sub` (строка 118), затем `format_text_with_incident` (utils.py:684-699 — текст не меняет, только приклеивает баннер инцидента).

2) Ключей нет нигде, кроме ru. Проверено импортом словарей: `main.welcome_no_sub` и `main.welcome_expired` есть только в `app/i18n/ru.py:625,626`; в en/de/kk/ar/uz/tj отсутствуют. Рантайм-проверка через `.venv/bin/python`: `get_text('en','main.welcome_no_sub')` → `'main.welcome_no_sub'`, то же для de/kk/ar/uz/tj и для `main.welcome_expired`. Логгер пишет «I18N missing key in all languages». Механизма слияния словарей нет (`LANG.update`/`setdefault` в app/i18n/ отсутствуют), fallback в `app/i18n/__init__.py:44-65` — только «язык → en → сам ключ», русского фолбэка нет.


_(обоснование сокращено, полностью — в findings.json)_

</details>

### 2. Экран подключения (setup_step1/step2) на не-ru языках состоит из сырых ключей

**P1** · ✅ подтверждено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:609`

**Дефект.** Живой flow подключения тянет ключи, которых нет ни в одном языке кроме ru: setup.install_app (609), setup.install_happ_ru (624), setup.install_happ_global (628), setup.next_step (652), setup.key_install_title (723), setup.btn_done (780), setup.btn_manual (785), setup.btn_need_help (790). Плюс тот же дефект на магазине — shop.title (1795), shop.apple_title (1908), shop.apple_amount_title (1931), shop.apple_confirm (1973), на помощи — help.menu_title (screens.py:142), help.faq_title (1868), help.contacts_title (1846) и на комбо — combo.screen_title/tariff_basic/tariff_plus/select_* (1486-1496).

**Когда ломается.** Юзер с language=en оплатил подписку, жмёт «Подключиться» → выбирает iPhone → видит текст «setup.install_app» и кнопки «setup.next_step», «setup.btn_done», «setup.btn_manual». Подключиться он не сможет и уйдёт в поддержку или в возврат.

**Что сделать.** Заполнить перечисленные ключи в en/de/kk/ar/uz/tj. Отдельно завести CI-проверку: любой ключ, запрошенный из app/handlers/**, обязан присутствовать во всех языках либо иметь явный ru-фолбэк.

**Уточнение проверки.** Две поправки к формулировке, не отменяющие находку. (а) «Подключиться он не сможет» — преувеличение: кнопки автонастройки на step2 (navigation.py:750-775 «Happ VPN»/«Incy VPN») и кнопки скачивания на step1 (621/636/645) захардкожены не через i18n и работают; ломается текст экрана и подписи нижних кнопок, а не сам механизм подключения. (б) Контекст: главное меню и так почти целиком захардкожено по-русски («Магазин», «Помощь», «Настройки» — app/handlers/common/keyboards.py:216-265; «🗣 Изменить язык» — navigation.py:237), так что не-ru пользователь и до этого экрана видит русский интерфейс; сырые ключи хуже, но это часть системного разрушения локализации (184 ru-ключа отсутствуют в en, включая целиком shop.*, help.*, combo.*), а не изолированный дефект setup-флоу. Побочно замечено рядом: navigation.py:234 и 243 вызывают i18n_get_text(language, key, "⚙️ Настройки") — третий позиционный аргумент это `strict`, а не default, поэтому «запасной» текст молча игнорируется.

<details><summary>Как это проверялось</summary>

Попытка опровергнуть не удалась — находка воспроизводится по коду и по данным.

1) Место и код совпадают. app/handlers/callbacks/navigation.py:609 `text = i18n_get_text(language, "setup.install_app")`, далее 624/628 (`setup.install_happ_ru`/`setup.install_happ_global`), 652 (`setup.next_step`); step2 — 723 (`setup.key_install_title`), 780/785/790 (`setup.btn_done`/`btn_manual`/`btn_need_help`). Магазин: 1795, 1908, 1931, 1973. Помощь: app/handlers/common/screens.py:142, navigation.py:1846, 1868. Комбо: navigation.py:1486-1488, 1492, 1496. Все указанные строки подтверждены дословно.

2) Ключей действительно нет нигде, кроме ru. Прогон через сам загрузчик (.venv/bin/python, `from app.i18n import LANGUAGES`) даёт для каждого из перечисленных ключей единственный язык — ru. Масштаб шире находки: в en отсутствует 184 ru-ключа, из них весь неймспейс shop.* (16/16), help.* (21/21), combo.* (11/11) и 19 из 51 setup.*. Языки uz/tj/de/kk/ar содержат по 810 ключей против 998 у ru.

3) Фолбэк не спасает. app/i18n/__init__.py:44-66: язык → en → «вернуть сам ключ». Поскольку ключа нет и в en, отдаётся строка вида "setup.install_app", логируется logger.error, исключения нет. Никакого рантайм-патча нет: translation_patch_*.json нигде не читаются (`grep -rn translation_patch --include=*.py` пусто).


_(обоснование сокращено, полностью — в findings.json)_

</details>

### 3. Мёртвый остров экранов: setup_platform / setup_key / весь QR-flow недостижимы

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: средний

`app/handlers/callbacks/navigation.py:927`

**Дефект.** Кластер обработчиков callback_setup_platform (927), callback_setup_key (1064), callback_setup_qr (1212), callback_setup_qr_standard (1267), callback_setup_qr_bypass (1280), _show_qr_app_choice (1293), callback_setup_qr_app (1321), _send_qr_screen (1390) — замкнут сам на себя. Единственные входы: кнопка setup_key:* (её нигде нет — grep по всему репозиторию, включая dashboard-генератор кнопок app/api/dashboard/routes/broadcasts.py, даёт только сам обработчик) и «Назад» с экранов setup_qr (строки 1229, 1260), которые сами достижимы только из setup_platform (строка 1047). Живой flow другой: connect_instruction → setup_step1 → setup_step2 → setup_manual/setup_done. Итого ~350 строк, включая всю фичу QR-кода подписки и генерацию через qrcode.

**Когда ломается.** Не воспроизводится пользователем: ни одна кнопка бота не отдаёт setup_key:{platform}, значит вход в setup_platform и далее в QR отсутствует. Фича QR-кода, i18n-ключи setup.qr_* и зависимость qrcode существуют, но пользователю недоступны.

**Что сделать.** Решить продуктово: либо вернуть вход в QR (кнопка «Добавить устройство» на экране setup_step2), либо удалить весь кластер вместе с ключами setup.qr_*, setup.combined_*, setup.manual_button, setup.qr_button и зависимостью qrcode. Не удалять по частям — обработчики ссылаются друг на друга.

### 4. Обработчик оплаты комбо с баланса не имеет ни одной кнопки

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:1639`

**Дефект.** callback_combo_pay_balance (F.data.startswith("combo_pay_balance:")) — ~95 строк, которые создают покупку, списывают баланс (database.decrease_balance, 1697), финализируют подписку и начисляют ГБ обхода. Кнопки combo_pay_balance:* нет нигде в репозитории. Живой путь другой: callback_combo_period (1580) кладёт combo_bypass_gb в FSM и отдаёт управление общему show_payment_method_selection, а ГБ начисляются уже в payments_messages.py:1254-1316.

**Когда ломается.** Не исполняется. Опасность в другом: код списывает баланс и параллельно вызывает finalize_purchase — если кто-то в будущем повесит на него кнопку, поведение разойдётся с общим платёжным конвейером (двойной учёт скидок, отдельная запись record_traffic_purchase, отдельный set_combo_flag).

**Что сделать.** Удалить обработчик целиком после подтверждения, что оплата комбо с баланса идёт общим путём (проверить FSM-ветку combo_bypass_gb в app/handlers/callbacks/payments_callbacks.py:623-786). Если нужен отдельный сценарий — пересобрать поверх общего конвейера, а не дублировать списание.

### 5. Экран «Статус сервиса» недостижим из бота

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:265`

**Дефект.** Обработчик callback_service_status (menu_service_status) существует, читает database.get_incident_settings() и отдаёт get_service_status_keyboard. Кнопки menu_service_status нет ни в одном файле — grep по всему репозиторию даёт только сам обработчик. get_service_status_keyboard (keyboards.py:551) вызывается только отсюда.

**Когда ломается.** Юзер не может посмотреть статус сервиса ни из главного меню, ни из настроек, ни из помощи. При активном инциденте баннер всё же показывается — через format_text_with_incident на главном экране (navigation.py:119), так что функционально дыры нет, но отдельный экран мёртв.

**Что сделать.** Либо вернуть кнопку «Статус сервиса» в menu_settings или menu_help, либо удалить обработчик вместе с get_service_status_keyboard и ключами main.service_status_text / main.incident_status_warning.

### 6. back_to_main — мёртвый дубль menu_main

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:76`

**Дефект.** callback_back_to_main делает ровно то же, что callback_main_menu (строка 39): clear FSM, delete, send_photo с _MAIN_PHOTO_ID и главным меню. Отличие только в отсутствии ensure_db_ready_callback. Кнопки back_to_main нет: единственные вхождения строки в репозитории — имя локальной функции _get_back_to_main_kb в app/handlers/payments/telegram_premium.py:80 (она отдаёт callback_data="menu_main").

**Когда ломается.** Не исполняется. При этом расхождение в поведении (нет DB-guard) означает, что если кнопку когда-то вернут, при неготовой БД экран упадёт на database.get_subscription внутри _get_main_text вместо аккуратного сообщения.

**Что сделать.** Удалить обработчик; если нужен алиас — свести к @router.callback_query(F.data.in_({"menu_main","back_to_main"})) на одном callback_main_menu.

### 7. go_profile — мёртвый дубль menu_profile

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:401`

**Дефект.** callback_go_profile повторяет callback_profile из subscription.py:333 (тот же clear FSM + show_profile), навешен двумя декораторами (401 и 402), из-за чего регистрируется в диспетчере дважды. Кнопки go_profile нет нигде — grep даёт только сам обработчик и его собственные log-строки.

**Когда ломается.** Не исполняется. Двойная регистрация одного и того же фильтра лишний раз проходится по цепочке роутера на каждом неподошедшем callback.

**Что сделать.** Удалить обработчик целиком. Если алиас нужен — добавить "go_profile" в фильтр callback_profile. Заодно убрать дублирующий декоратор StateFilter(default_state) — он избыточен, второй декоратор без фильтра всё равно ловит всё.

### 8. Кнопка «Политика конфиденциальности» на экране самой политики ведёт на себя же и молча не срабатывает

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:299`

**Дефект.** callback_privacy рендерит текст main.privacy_policy_text с клавиатурой get_about_keyboard(language). Эта клавиатура (keyboards.py:535-539) первым рядом содержит кнопку с callback_data="about_privacy" — то есть ссылку на текущий экран. safe_edit_text (app/handlers/common/utils.py:461-476) при совпадении текста и разметки выходит досрочно, ничего не отправляя.

**Когда ломается.** Юзер на экране политики жмёт «Политика конфиденциальности» — в чате не происходит ничего. Кнопка выглядит сломанной.

**Что сделать.** Сделать для экрана политики отдельную клавиатуру без самоссылки (только «Наш канал» и «Назад»), либо на этом экране менять «Назад» на возврат в menu_settings, откуда юзер пришёл.

### 9. Кнопки «Назад» сбрасывают юзера в корень вместо предыдущего экрана

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: средний

`app/handlers/callbacks/navigation.py:144`

**Дефект.** Экран «Наша экосистема» вызывается из menu_settings (кнопка на строке 241), но его «Назад» ведёт на menu_main (строка 144). Экран «О сервисе» вызывается из экосистемы (строка 142), а get_about_keyboard возвращает на menu_main (keyboards.py:545). Экран политики достижим из menu_settings (строка 238) и тоже возвращает на menu_main. То есть цепочка Главная → Настройки → Экосистема → О сервисе схлопывается одним «Назад» до корня.

**Когда ломается.** Юзер зашёл в Настройки → Экосистема, прочитал, жмёт «Назад» — оказывается на главной, а не в Настройках. Чтобы вернуться к соседнему пункту настроек, приходится проходить путь заново. То же на трёх уровнях подряд.

**Что сделать.** Не менять кнопки сейчас, а зафиксировать правило: «Назад» ведёт на экран-родитель. Для этого get_about_keyboard нужен параметр back_to (menu_main / menu_settings / menu_ecosystem), а menu_ecosystem — возврат на menu_settings, если вход был оттуда. Альтернатива — хранить стек экранов в FSM.

### 10. Третий позиционный аргумент i18n_get_text — это strict, а не default; «значение по умолчанию» нигде не работает

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: средний

`app/handlers/callbacks/navigation.py:138`

**Дефект.** Сигнатура get_text(language, key, strict=None, **kwargs) (app/i18n/__init__.py:31). Код повсеместно передаёт третьим позиционным аргументом строку-заглушку, считая её default: navigation.py:138,139,235,276,280,298,473,482; keyboards.py:290,537,559,578; screens.py:114,167; subscription.py:388,496,500,503-506. Аргумент попадает в strict, который помечен deprecated и не используется. При отсутствии ключа функция возвращает сам ключ (строка 64), а не заглушку.

**Когда ломается.** Разработчик пишет i18n_get_text(language, "main.service_status_text", "service_status_text") в уверенности, что при пропаже ключа юзер увидит осмысленный текст. На деле увидит строку «main.service_status_text». Именно этот механизм даёт эффект из BASELINE (errors.database_unavailable → сырой ключ у юзера).

**Что сделать.** Либо добавить в get_text настоящий параметр default (и заменить strict на keyword-only), либо вычистить все третьи позиционные аргументы, чтобы код не создавал ложного ощущения защиты. Второй вариант дешевле и честнее — заглушки всё равно повторяют ключ.

### 11. Экран «Инструкция» практически недостижим, при этом в боте два разных экрана инструкции

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: средний

`app/handlers/callbacks/navigation.py:392`

**Дефект.** callback_instruction обслуживает menu_instruction и instruction. Алиас instruction — сирота (кнопки нет нигде). Сам menu_instruction эмитируется только из get_reissue_notification_keyboard (keyboards.py:619 и её мёртвый дубль handlers.py:825) и из мёртвого главного меню в handlers.py:647 — то есть из главного меню обычного юзера этот экран недоступен. Параллельно живёт второй, другой «инструкционный» экран: menu_help → «📲 Инструкции по сервису» → connect_instruction (screens.py:145). Первый ведёт в Mini App guide (get_instruction_keyboard, keyboards.py:578), второй — в пошаговую установку в боте.

**Когда ломается.** Юзер видит экран Mini App-инструкции только если админ перевыпустил ему ключ. Обычный путь ведёт на другой экран с другим содержанием — два конкурирующих ответа на один вопрос «как подключиться».

**Что сделать.** Выбрать один канонический экран инструкции. Если это Mini App guide — повесить кнопку в menu_help вместо connect_instruction. Если это пошаговая установка — удалить callback_instruction, get_instruction_keyboard и ключи instruction._text / instruction._open_guide, а в reissue-уведомлении заменить menu_instruction на connect_instruction.

### 12. Кнопки «Скопировать логин» и «Скопировать пароль» в бизнес-панели отдают одно и то же значение

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:200`

**Дефект.** callback_biz_copy_login (200-210) и callback_biz_copy_password (213-223) читают один и тот же sub.get("vpn_key") и отправляют его одинаковым сообщением <code>{vpn_key}</code>. Обе кнопки живые — get_biz_control_panel_keyboard (keyboards.py:321-328). Комментарий у второй функции («VPN ключ Plus») расходится с кодом.

**Когда ломается.** Бизнес-подписчик жмёт «Скопировать пароль», получает ту же ссылку подписки, что и по «Скопировать логин». Он либо считает бота сломанным, либо решает, что пароль утерян, и идёт к персональному менеджеру.

**Что сделать.** Определить, есть ли у бизнес-подписки отдельные логин и пароль. Если нет — оставить одну кнопку «Скопировать ссылку подключения». Если есть — брать пароль из соответствующего поля, а не из vpn_key.

### 13. Экран установки на шаге 2 превращается в тупик для юзера без подписки

**P2** · ⚠️ не перепроверено · уверенность автора находки: medium · риск правки: низкий

`app/handlers/callbacks/navigation.py:739`

**Дефект.** callback_setup_step2 добавляет ряды кнопок с deeplink'ами только при непустом sub_url (условие на строке 739). Экран при этом достижим без подписки: menu_help → «📲 Инструкции по сервису» → connect_instruction (screens.py:145) → выбор устройства → setup_step1 → «Дальше» → setup_step2.

**Когда ломается.** Юзер без подписки заходит в Помощь, чтобы понять как всё работает, доходит до экрана «Подключитесь в одно нажатие» и видит заголовок про одно нажатие и три кнопки — «Готово», «Установить вручную», «Нужна помощь». Ни ключа, ни объяснения, ни CTA на покупку. «Установить вручную» покажет пустой экран без ключей (setup_manual, строки 1132-1146).

**Что сделать.** Добавить на setup_step2 и setup_manual явную ветку «подписки нет»: короткий текст + кнопка «Купить подписку» (menu_buy_vpn) вместо кнопок-заглушек. Кнопки не менять сейчас, зафиксировать в бэклоге как правило: любой экран установки обязан иметь осмысленное состояние без sub_url.

### 14. Экран «VIP-доступ» недостижим

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/subscription.py:380`

**Дефект.** callback_vip_access (F.data == "menu_vip_access") строит экран с текстом main.vip_access_text и кнопкой «Назад» на menu_profile. Кнопки menu_vip_access нет ни в одном keyboard-билдере: grep по репозиторию находит только сам обработчик и упоминание в docstring модуля (строка 3).

**Когда ломается.** VIP-статус выдаётся админом (admin:vip_grant, keyboards.py:724), но у юзера нет ни одного экрана, где он про этот статус узнает. Скидка 30% при этом применяется молча (subscription.py:439-440).

**Что сделать.** Решить, живой ли продукт VIP. Если да — вернуть кнопку в профиль для is_vip-юзеров. Если нет — удалить обработчик и ключи main.vip_access_text / main.vip_status_active / main.contact_manager_button.

### 15. Экран «История подписок» недостижим

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/subscription.py:485`

**Дефект.** callback_subscription_history (F.data == "subscription_history") выбирает database.get_subscription_history и рендерит список. Кнопки subscription_history нет — единственные вхождения в репозитории это admin:user_history (админская история чужого юзера, handlers.py:921 и keyboards.py:712) и имя таблицы в SQL.

**Когда ломается.** Юзер не может посмотреть свою историю покупок из бота. При этом обработчик печатает в текст record['vpn_key'] (строка 527) — то есть выдал бы сырой ключ, что противоречит текущей политике «ключи в боте не отдаём» (см. navigation.py:441).

**Что сделать.** Удалить обработчик; если история нужна продукту — переписать без вывода vpn_key и повесить кнопку в профиль.

### 16. callback_get_sub_key — мёртвый экран, выдающий ссылку подписки в обход текущей политики

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/subscription.py:457`

**Дефект.** Обработчик находится в navigation.py:457 (F.data == "get_sub_key"): печатает <code>{sub_url}</code> с инструкцией. Кнопки get_sub_key нет нигде. Соседний обработчик callback_connect_instead_of_copy (navigation.py:439-454) явно документирует обратное правило: «Ключи больше не отправляются в боте». Кроме того, единственная кнопка setup_device во всём репозитории живёт именно на этом мёртвом экране (строка 493) — из-за чего мёртв и callback_setup_device (navigation.py:887).

**Когда ломается.** Не исполняется. При случайном возврате кнопки экран нарушил бы принятую политику неотдачи ключей и продублировал бы экран выбора устройства.

**Что сделать.** Удалить callback_get_sub_key вместе с ключом get_key.instruction_text и обработчиком callback_setup_device (он полностью дублирует device-picker из callback_connect_instruction, строки 559-572 против 899-912).

### 17. Юзер без подписки не может из меню попасть в профиль, баланс, рефералку и настройки

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/common/keyboards.py:252`

**Дефект.** В get_main_menu_keyboard вся ветка else (строки 252-265) для юзеров без активной подписки даёт только «Магазин» и «Помощь» (плюс выше — триал/спецоффер/«Купить подписку»/«Только обход»/прокси). Кнопок menu_profile, menu_referral, menu_settings в этой ветке нет — они добавляются только внутри if has_active_sub (строки 190, 234, 242). Профиль — единственный экран, где живут «Пополнить» (keyboards.py:425), «Мои подарки» (433) и смена языка (431).

**Когда ломается.** Юзер, у которого подписка истекла, хочет пополнить баланс, чтобы купить — кнопки нет. Юзер, получивший подарок (my_gifts), не найдёт его. Реферальная программа «Круг Амбассадоров» (screens.py:176) невидима именно для тех, кто ещё не купил — то есть ростовой цикл отключён у самой массовой когорты. Обходной путь есть только через команды /profile и /referral, о которых из интерфейса ничего не сообщается.

**Что сделать.** Не меняя код сейчас: зафиксировать, что в ветке без подписки должны быть как минимум «Личный кабинет» (он же баланс/подарки/язык) и «Заработать с нами». Проверить также, зарегистрированы ли /profile и /referral в меню команд BotFather — сейчас они существуют только как обработчики (app/handlers/user/profile.py:20, referrals.py:22).

### 18. Тексты главного меню и профиля захардкожены по-русски и игнорируют язык юзера

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: средний

`app/handlers/common/keyboards.py:134`

**Дефект.** get_main_menu_keyboard принимает language, но почти все подписи кнопок — русские литералы: «Подключиться» (135), «🎁 Попробовать бесплатно — 3 дня» (151), «Продлить со скидкой 15%» (163), «Купить подписку» (176), «🌐 Только обход блокировок» (181), «Купить ГБ обхода»/«Купить VPN» (199,204), «Продлить подписку»/«Подарить» (212,217), «Игровой клуб»/«Магазин» (224,229), «Заработать с нами» (235), «Настройки»/«Помощь» (243,247,257,261), «Мой прокси»/«Telegram MT Прокси» (117). То же в get_profile_keyboard (368,374,382,388,398,418,425,426,431,440) и в экране помощи (screens.py:144-147).

**Когда ломается.** Юзер выбрал English — заголовки экранов частично переводятся, а все кнопки остаются русскими. В сочетании с находкой про сырые ключи получается смесь трёх языков на одном экране.

**Что сделать.** Завести ключи под каждую подпись главного меню и профиля и заполнить их во всех семи языках. Отдельно — запретить литералы в keyboards.py правилом линтера (например, ruff-плагин на не-ASCII строковые литералы в файлах билдеров клавиатур).

### 19. Четыре мёртвые кнопки broadcast_type:* и мёртвый билдер, который их создаёт

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/common/keyboards.py:646`

**Дефект.** get_broadcast_type_keyboard создаёт кнопки broadcast_type:info|maintenance|security|promo (строки 649-652). Обработчика с таким префиксом в репозитории нет. Сама функция никем не вызывается — единственные ссылки это ре-экспорт в app/handlers/common/__init__.py:38 и её точная копия в handlers.py:856-866 (тоже без вызывающих). Актуальный админский flow рассылок использует другой набор (app/handlers/admin/keyboards.py).

**Когда ломается.** Не исполняется: клавиатура никуда не подставляется. Если бы подставилась — админ жал бы по кнопке и получал вечный «часики» без ответа (aiogram не отвечает на callback без обработчика).

**Что сделать.** Удалить get_broadcast_type_keyboard из keyboards.py и из __init__, а также её копию в handlers.py. Проверить заодно get_broadcast_test_type_keyboard, get_broadcast_segment_keyboard, get_broadcast_confirm_keyboard — они там же и тоже без вызывающих в app/.

### 20. Пять неиспользуемых билдеров клавиатур в keyboards.py, часть из них — заглушки-обманки

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/common/keyboards.py:455`

**Дефект.** Без вызывающих (кроме ре-экспорта в app/handlers/common/__init__.py): get_profile_keyboard_with_copy (455) — игнорирует три своих параметра и просто зовёт get_profile_keyboard; get_profile_keyboard_old (460) — единственный эмитент мёртвого copy_key; get_vpn_key_keyboard (480) — игнорирует subscription_type и vpn_key, возвращает get_connect_keyboard(); get_tariff_keyboard (508) — помечена DEPRECATED в докстринге, строит ключи "buy.tariff_button_" + tariff_key, которых нет в ru (единственный ru-MISS в моём домене, keyboards.py:516); get_connect_button (33).

**Когда ломается.** Не исполняется. Опасность в сигнатурах-обманках: get_vpn_key_keyboard принимает vpn_key и молча его игнорирует — вызывающий будет уверен, что кнопка с ключом есть. get_tariff_keyboard при вызове отрисовала бы кнопки с текстом «buy.tariff_button_basic».

**Что сделать.** Удалить все пять вместе с их строками в app/handlers/common/__init__.py (24,27,28,29,30 и __all__ 65,68,69,70,71). Это же закрывает пункт baseline про buy.tariff_button_ — ключ действительно динамический и действительно мёртвый.

### 21. handlers.py дублирует весь легаси-набор экранов главного меню и профиля

**P2** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: средний

`handlers.py:610`

**Дефект.** В корневом handlers.py живут вторые копии get_main_menu_keyboard (610), get_profile_keyboard (681), get_about_keyboard (740), get_service_status_keyboard (759), get_instruction_keyboard (776), get_reissue_notification_keyboard (822), get_language_keyboard (568), get_back_keyboard (671) и четырёх broadcast-клавиатур (846-886). Ни одна из них не вызывается: из handlers.py во всём проекте импортируется только show_payment_method_selection (app/handlers/payments/callbacks.py:20, navigation.py:1635, admin/broadcast.py:464,673,1010). Копии разошлись с оригиналами — в handlers.py:637-660 главное меню ещё содержит кнопки menu_instruction и menu_ecosystem, которых в живом меню нет.

**Когда ломается.** Не исполняется. Именно эта копия даёт половину «мёртвых кнопок» из baseline (broadcast_type:* в handlers.py:859-862) и заставляет считать menu_instruction/menu_ecosystem достижимыми из главного меню, хотя это не так.

**Что сделать.** Вынести show_payment_method_selection в app/handlers/payments/, после чего удалить из handlers.py весь блок keyboard-билдеров. Это отдельная задача от удаления xray-ветки, но её стоит делать в том же заходе — оба перевода легаси-мостов.

### 22. copy_key_menu / copy_key_plus / copy_key — мёртвые элементы фильтра

**P3** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:439`

**Дефект.** Фильтр F.data.in_({"copy_key_menu", "copy_key", "copy_key_plus", "copy_vpn_key"}) содержит четыре значения, из которых живо одно: copy_vpn_key (кнопка в get_reissue_notification_keyboard, keyboards.py:620, вызывается из app/handlers/admin/reissue.py:65 и admin/access.py:152,2144). copy_key встречается только в get_profile_keyboard_old (keyboards.py:476) — а эта функция сама никем не вызывается. copy_key_menu и copy_key_plus не встречаются нигде.

**Когда ломается.** Не исполняется. Скрипт сверки справедливо пометил их сиротами — это не ложное срабатывание, а забытый рефакторинг после отказа от выдачи ключей в боте.

**Что сделать.** Сократить фильтр до F.data == "copy_vpn_key" и удалить get_profile_keyboard_old.

### 23. Обработчик noop зарегистрирован дважды, админская версия недостижима

**P3** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/callbacks/navigation.py:33`

**Дефект.** F.data == "noop" объявлен в navigation.py:33 и в app/handlers/admin/base.py:938. В app/handlers/__init__.py:18-21 callbacks_router подключается раньше admin_router, а внутри callbacks/__init__.py:13 navigation_router идёт первым — значит выигрывает всегда navigation-версия, а админская мертва. Декоративные noop-кнопки есть и в админке (keyboards.py:733 «⏳ Перевыпуск...»), и в пользовательском flow (navigation.py:1019-1022, экран мёртвого setup_platform).

**Когда ломается.** Функционально безвредно (обе версии делают callback.answer()), но если админская версия когда-нибудь получит своё поведение (например, логирование), оно молча не сработает.

**Что сделать.** Удалить обработчик из admin/base.py:938, оставив один общий в navigation.py.

### 24. Пустая ветка-заглушка в построении главного меню

**P3** · ⚠️ не перепроверено · уверенность автора находки: high · риск правки: низкий

`app/handlers/common/keyboards.py:171`

**Дефект.** В get_main_menu_keyboard блок «if not offer_shown and not trial_available: pass» (строки 171-173) с комментарием «Нет триала и нет спецпредложения — обычные кнопки» ничего не делает. Переменная offer_shown после этого нигде не используется.

**Когда ломается.** Не влияет на поведение. Это след недоделанного условия: судя по комментарию, предполагалось разное меню для «нет триала и нет оффера», но ветка осталась пустой.

**Что сделать.** Удалить блок вместе с переменной offer_shown либо дописать задуманное поведение, если продукт его всё ещё хочет.

---

## Опровергнутые находки

Скептик показал, что эти находки неверны. Оставлены для истории.

- **Открытие экрана тарифов гасит уже созданный инвойс — оплата отклоняется, деньги остаются у провайдера** (`app/handlers/common/screens.py:597`) — Фактическая часть находки верна, но ключевое звено сценария («Telegram списывает деньги») опровергается кодом pre-checkout.

Что подтвердилось:
- app/handlers/common/screens.py:597 — да, `await database.cancel_pending_purchases(telegram_id, "new_purchase_started")` вызывается безусловно при каждом открытии экрана тарифов (вызовы: navigation.py:326/357/388, payments/callbacks.py:80, payments/buy.py:23, screens.py:706).
- database/subscriptions.py:4287-4290 — переводит ВСЕ 'pending' покупки юзера в status='expired'.
- database/subscriptions.py:4232-4235 — ветка `check_expiry=False` действительно требует `status = 'pending'`, поэтому не найдёт 'expired'.
- app/handlers/payments/payments_messages.py:521-543 — при отсутствии покупки отвечает `errors.session_expired` и делает return.

Что опровергает сценарий:
- app/handlers/payments/payments_messages.py:54-80 — зарегистрирован единственный `@

---

## Что прочитано, а что нет

Прочитано целиком: app/handlers/callbacks/navigation.py (2228 строк, двумя проходами), app/handlers/common/screens.py (706), app/handlers/common/keyboards.py (752), app/handlers/callbacks/subscription.py (532), app/handlers/user/start.py (977). Прочитано выборочно (для проверки достижимости и связок): handlers.py (структура + строки 610-980), app/handlers/common/utils.py (safe_edit_text, 407-500), app/i18n/__init__.py целиком, app/handlers/user/{profile,support,connect,language_commands}.py, app/handlers/common/__init__.py, app/handlers/{__init__,callbacks/__init__,admin/__init__,payments/__init__}.py (порядок роутеров), database/subscriptions.py (4202-4320: get_pending_purchase / cancel_pending_purchases), app/handlers/payments/payments_messages.py (505-545, 1254-1316), app/api/dashboard/routes/broadcasts.py (генератор кнопок рассылок). Достижимость каждого callback_data проверена AST-независимым grep'ом по всем .py репозитория (исключая graphify-out) плюс скриптом, который сверил все литеральные callback_data из моих пяти файлов с множеством exact/startswith/in_-фильтров — расхождений, кроме broadcast_type:*, нет. i18n-покрытие проверено загрузкой всех семи словарей и сверкой ключей, запрашиваемых из моих файлов. НЕ читалось: app/handlers/payments/* (кроме указанных фрагментов), app/handlers/admin/* (кроме проверки эмитентов кнопок), app/handlers/game.py, traffic.py, proxy.py, gift.py, devices.py — их экраны и «Назад» не проверялись, поэтому находки про циклы навигации ограничены моим доменом. Продовых логов и БД нет, поэтому мёртвость везде доказана только недостижимостью по коду; для находки про cancel_pending_purchases (P0) путь оплаты подтверждён по коду, но реальная частота сценария неизвестна — её должен подтвердить домен платежей.
