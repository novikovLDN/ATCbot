# Карта мёртвого кода

Кандидатов: **42**.

Продовых логов и доступа к БД нет, поэтому мёртвость доказывается только недостижимостью по коду. Всё, что помечено уверенностью `low`, требует решения владельца, а не автоматического удаления.

| Уверенность | Статус | Место | Что | Действие |
|---|---|---|---|---|
| high | подтверждено | `database/core.py:457` | Две системы управления схемой БД: 68 SQL-миграций и 116 DDL-операторов в database/core.py на каждом старте | Признать migrations/*.sql единственным источником схемы. Вынести DDL-блок database/core.py:481-1160 в отдельную миграцию-бутстрап (или скрипт для лока |
| high | не проверено | `auto_renewal.py:215` | Автопродление считает цену по своей формуле мимо calculate_final_price и админских override | Заменить блок auto_renewal.py:211-224 вызовом app.services.subscriptions.service.calculate_price с теми же tariff/period_days и списывать final_price_ |
| high | не проверено | `app/core/i18n/__init__.py:7` | Пакет app/core/i18n сломан на импорте и никем не используется | Удалить директорию app/core/i18n целиком. Плюрализация из types.py (_ar_plural и константы PLURAL_*), если нужна, переносится в app/i18n/. |
| high | не проверено | `app/handlers/admin/audit_subs.py:90` | audit_subs.py и audit_db_dates.py — почти клоны (1359 строк на два варианта одного аудита) | Вынести общий каркас в app/handlers/admin/_audit_base.py: _compute_real_end, _iso_z, _parse_rmn_dt, обёртки воркера с прогресс-сообщением и общий _for |
| high | не проверено | `app/handlers/callbacks/navigation.py:927` | Мёртвый остров экранов: setup_platform / setup_key / весь QR-flow недостижимы | Решить продуктово: либо вернуть вход в QR (кнопка «Добавить устройство» на экране setup_step2), либо удалить весь кластер вместе с ключами setup.qr_*, |
| high | не проверено | `app/handlers/callbacks/navigation.py:1639` | Обработчик оплаты комбо с баланса не имеет ни одной кнопки | Удалить обработчик целиком после подтверждения, что оплата комбо с баланса идёт общим путём (проверить FSM-ветку combo_bypass_gb в app/handlers/callba |
| high | не проверено | `app/handlers/callbacks/navigation.py:265` | Экран «Статус сервиса» недостижим из бота | Либо вернуть кнопку «Статус сервиса» в menu_settings или menu_help, либо удалить обработчик вместе с get_service_status_keyboard и ключами main.servic |
| high | не проверено | `app/handlers/callbacks/navigation.py:76` | back_to_main — мёртвый дубль menu_main | Удалить обработчик; если нужен алиас — свести к @router.callback_query(F.data.in_({"menu_main","back_to_main"})) на одном callback_main_menu. |
| high | не проверено | `app/handlers/callbacks/navigation.py:401` | go_profile — мёртвый дубль menu_profile | Удалить обработчик целиком. Если алиас нужен — добавить "go_profile" в фильтр callback_profile. Заодно убрать дублирующий декоратор StateFilter(defaul |
| high | не проверено | `app/handlers/callbacks/subscription.py:380` | Экран «VIP-доступ» недостижим | Решить, живой ли продукт VIP. Если да — вернуть кнопку в профиль для is_vip-юзеров. Если нет — удалить обработчик и ключи main.vip_access_text / main. |
| high | не проверено | `app/handlers/callbacks/subscription.py:485` | Экран «История подписок» недостижим | Удалить обработчик; если история нужна продукту — переписать без вывода vpn_key и повесить кнопку в профиль. |
| high | не проверено | `app/handlers/callbacks/subscription.py:457` | callback_get_sub_key — мёртвый экран, выдающий ссылку подписки в обход текущей политики | Удалить callback_get_sub_key вместе с ключом get_key.instruction_text и обработчиком callback_setup_device (он полностью дублирует device-picker из ca |
| high | не проверено | `app/handlers/common/keyboards.py:646` | Четыре мёртвые кнопки broadcast_type:* и мёртвый билдер, который их создаёт | Удалить get_broadcast_type_keyboard из keyboards.py и из __init__, а также её копию в handlers.py. Проверить заодно get_broadcast_test_type_keyboard,  |
| high | не проверено | `app/handlers/common/keyboards.py:455` | Пять неиспользуемых билдеров клавиатур в keyboards.py, часть из них — заглушки-обманки | Удалить все пять вместе с их строками в app/handlers/common/__init__.py (24,27,28,29,30 и __all__ 65,68,69,70,71). Это же закрывает пункт baseline про |
| high | не проверено | `app/handlers/common/keyboards.py:589` | Админские клавиатуры существуют в трёх копиях; используется только версия admin/keyboards.py | Удалить админские клавиатуры из app/handlers/common/keyboards.py (строки 589-746) и соответствующие реэкспорты из common/__init__.py. Оставить app/han |
| high | не проверено | `app/handlers/traffic.py:231` | app/handlers/traffic.py: два обработчика с мёртвым legacy-телом после return | Удалить блоки app/handlers/traffic.py:231-259 и 820-838. Если старая логика нужна как справка — она есть в git-истории. |
| high | не проверено | `app/services/payments/confirmation.py:110` | Смешанное обращение к слою: часть кода зовёт database.finalize_purchase напрямую в обход сервиса | Определить правило: database/* вызывается только из app/services/*, хендлеры ходят через сервисы. Перевести app/services/payments/confirmation.py:110, |
| high | не проверено | `app/services/vpn_client.py:1` | app/services/vpn_client.py — модуль без единого потребителя | Удалить файл целиком в подпроекте B. Потребителей вне xray-пути нет. |
| high | не проверено | `app/services/vpn_client.py:1` | app/services/vpn_client.py — 251 строк мёртвого фасада к декоммиссированному xray | Удалить app/services/vpn_client.py вместе с веткой xray (решение владельца #1). Убедиться, что тесты не мокают этот модуль. |
| high | не проверено | `app/utils/audit.py:179` | app/utils/audit.py — 418 строк мёртвой параллельной подсистемы аудита | Удалить app/utils/audit.py целиком. Если нужны редакция PII и типизированные события — перенести redact_metadata и AUDIT_EVENT_TYPES внутрь database/_ |
| high | не проверено | `app/utils/security.py:194` | Четыре реализации проверки «я админ» плюс 203 инлайновых сравнения с ADMIN_TELEGRAM_ID | Оставить одну реализацию is_admin (app/services/admin_auth.py:241) и один декоратор admin_only, механически заменить 203 инлайновых сравнения на вызов |
| high | не проверено | `database/admin.py:1` | Разбиение database/admin.py (4917 строк) по ответственностям | Разрезать на пакет database/admin/ : (1) analytics.py — 222,510,564,594,653,690,825,881,1092,1147,1229,3505,3524,3542,3567,3593,3883,3961,4033,4061,41 |
| high | не проверено | `database/subscriptions.py:2362` | Мёртвый legacy-API ручной модерации платежей в database/subscriptions.py (~350 строк, деньги) | Подтвердить у владельца, что ручная модерация платежей выведена из эксплуатации. Затем удалить create_payment, get_pending_payment_by_user, update_pay |
| high | не проверено | `database/subscriptions.py:1` | Разбиение database/subscriptions.py (5112 строк) по ответственностям | Разрезать на пакет database/subscriptions/ со следующими модулями, границы уже проходят по существующим блокам: (1) payments_legacy.py — 72,90,150,160 |
| high | не проверено | `handlers.py:610` | handlers.py дублирует весь легаси-набор экранов главного меню и профиля | Вынести show_payment_method_selection в app/handlers/payments/, после чего удалить из handlers.py весь блок keyboard-билдеров. Это отдельная задача от |
| high | не проверено | `handlers.py:39` | handlers.py — 950 из 1072 строк дублируют app/handlers/common/*, снаружи используется одна функция | Перенести show_payment_method_selection в app/handlers/payments/ (по смыслу — рядом с payments/callbacks.py), переключить пять импортов на новое место |
| high | не проверено | `vpn_utils.py:407` | upgrade_vless_user и remove_plus_inbound: мёртвые и при этом НЕ закрытые флагом | Удалить обе функции в подпроекте B вместе с остальным содержимым vpn_utils. Потребителей вне xray-пути нет. |
| high | не проверено | `xray_api/main.py:1` | Инвентарь удаления: что уносит xray-ветка (файлы и конфиг) | Порядок работ: (1) переопределить VPN_ENABLED через REMNAWAVE_ENABLED; (2) перевести fast_expiry_cleanup, trial_notifications, auto_renewal, admin_rev |
| high | не проверено | `xray_sync.py:93` | xray_sync.py — воркер, который вхолостую обходит всех активных пользователей | Удалить xray_sync.py, XRAY_SYNC_ENABLED (config.py:371), блок импорта main.py:39-44, ветку авто-восстановления main.py:440-444, start_xray_sync_safe m |
| high | не проверено | `xray_sync.py:60` | Ветка xray (xray_sync.py, xray_api/, scripts/full_xray_resync.py) держится в дереве, но по решению владельца мертва | По решению владельца #1 удалить: xray_sync.py, директорию xray_api/, scripts/full_xray_resync.py, флаг XRAY_SYNC_ENABLED и блок main.py:38-44, 536-557 |
| high | не проверено | `app/handlers/callbacks/gift.py:48` | _schedule_invoice_deletion скопирована в пять модулей с расходящимися сигнатурами | Вынести один _schedule_invoice_deletion(bot, chat_id, message_id, timeout) в app/utils/telegram_safe.py (модуль уже существует), заменить пять копий и |
| high | не проверено | `app/handlers/callbacks/navigation.py:439` | copy_key_menu / copy_key_plus / copy_key — мёртвые элементы фильтра | Сократить фильтр до F.data == "copy_vpn_key" и удалить get_profile_keyboard_old. |
| high | не проверено | `app/handlers/callbacks/navigation.py:33` | Обработчик noop зарегистрирован дважды, админская версия недостижима | Удалить обработчик из admin/base.py:938, оставив один общий в navigation.py. |
| high | не проверено | `app/handlers/common/__init__.py:1` | app/handlers/common/__init__.py — фасад реэкспортов, которым никто не пользуется | Свести app/handlers/common/__init__.py к пустому docstring, затем прогнать анализ мёртвого кода заново и удалить проявившиеся функции: get_connect_but |
| high | не проверено | `app/handlers/common/keyboards.py:646` | get_broadcast_type_keyboard и её кнопки broadcast_type:* мертвы с обеих сторон | Удалить get_broadcast_type_keyboard из всех трёх файлов и связанные i18n-ключи broadcast_type_*. Если типизация рассылок нужна как фича — заводить зан |
| high | не проверено | `app/utils/message_guard.py:24` | app/utils/message_guard.py — мёртвый модуль защиты от edit_text по фото-сообщениям | Удалить app/utils/message_guard.py. Если нужна is_photo_message — перенести её в app/handlers/common/utils.py рядом с safe_edit_text. |
| high | не проверено | `database/core.py:839` | Таблица vpn_keys — реликт пула ключей, ни одного обращения из кода | Удалить CREATE TABLE vpn_keys из database/core.py:837-846 и добавить миграцию DROP TABLE IF EXISTS vpn_keys после подтверждения у владельца, что данны |
| medium | не проверено | `database/reconciliation.py:114` | Точечно мёртвые функции: _bulk_fetch_panel_expires_at, get_biz_price_stars, site_sync.periodic_sync, admin_callbacks.py | По каждой принять решение отдельно: _bulk_fetch_panel_expires_at, get_biz_price_stars, create_default_system_state, get_last_pool_wait_spike_monotonic |
| high | не проверено | `database/subscriptions.py:2660` | Мёртвая database.mark_reminder_sent и legacy-колонка subscriptions.reminder_sent | Удалить database.mark_reminder_sent (subscriptions.py:2660-2667) и снять экспорт из database/__init__.py:141. Отдельным шагом решить судьбу колонки re |
| high | не проверено | `migrations/036_notification_overhaul.sql:39` | Таблицы admin_notification_log и admin_notification_templates создаются и никогда не используются | Подтвердить у владельца, что шаблоны админских уведомлений не планируются, и добавить миграцию DROP TABLE для обеих. Если планируются — зафиксировать  |
| high | не проверено | `vpn_utils.py:84` | Мёртвые символы VPN-домена: исключения, алерт, хелперы, импорт | Удалить перечисленные символы вместе с xray-веткой. Единственное, что требует внимания за пределами домена, — app/core/__init__.py:2-4, его реэкспорт  |
| high | не проверено | `vpn_utils.py:407` | vpn_utils.upgrade_vless_user и remove_plus_inbound — мёртвые вызовы к декоммиссированному API, без no-op guard | Удалить обе функции вместе с остальной xray-веткой (решение владельца #1). Апгрейд тарифа сегодня делается через app/services/remnawave_premium. |
