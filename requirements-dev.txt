# Development / testing dependencies (not included in production Docker image)
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.9.0
# Static analysis + dependency scanning. Run via .github/workflows/ci.yml.
bandit>=1.7.5
pip-audit>=2.7.0
