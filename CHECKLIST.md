# Чеклист безопасности перед деплоем

Используйте этот чеклист перед коммитом в GitHub и деплоем на Railway.

## ✅ Проверка кода

- [ ] В `config.py` НЕТ хардкода токенов
- [ ] `config.py` использует `os.getenv()` для получения секретов
- [ ] `config.py` валидирует переменные окружения (падает с ошибкой, если не заданы)
- [ ] В `main.py` нет проверок на дефолтные значения токенов (валидация в config.py)
- [ ] Все импорты корректны

## ✅ Проверка файлов

- [ ] Существует `.gitignore`
- [ ] `.gitignore` содержит `.env`
- [ ] `.gitignore` содержит `*.db`, `*.sqlite`
- [ ] `.gitignore` содержит `__pycache__/`
- [ ] В репозитории НЕТ файла `.env`
- [ ] В репозитории НЕТ файлов `*.db` или `*.sqlite`
- [ ] `requirements.txt` содержит все зависимости

## ✅ Проверка GitHub репозитория

- [ ] Репозиторий доступен: https://github.com/novikovLDN/ATCbot.git
- [ ] В истории коммитов НЕТ секретов
- [ ] Все файлы закоммичены (кроме тех, что в .gitignore)

## ✅ Проверка перед деплоем на Railway

- [ ] У вас есть токен бота от @BotFather
- [ ] Вы знаете свой Telegram ID (можно узнать у @userinfobot)
- [ ] Аккаунт на Railway создан
- [ ] Репозиторий подключен к Railway

## ✅ Проверка после деплоя

- [ ] Переменные окружения `BOT_TOKEN` и `ADMIN_TELEGRAM_ID` добавлены в Railway
- [ ] Логи показывают "База данных инициализирована" и "Бот запущен"
- [ ] Нет ошибок в логах
- [ ] Бот отвечает на `/start`
- [ ] Админ получает уведомления о платежах

## 🔒 Безопасность

✅ **Секреты НИКОГДА не должны быть:**
- В коде
- В коммитах
- В .env файлах, которые коммитятся
- В Issues/Pull Requests
- В логах (кроме локальных для отладки)

✅ **Секреты ДОЛЖНЫ быть:**
- Только в переменных окружения Railway
- Только в локальном .env файле (не коммитится)

## 📝 Команды для проверки

```bash
# Проверить, что нет .env файлов
find . -name ".env" -o -name "*.db" -o -name "*.sqlite"

# Проверить, что нет токенов в коде
grep -r "BOT_TOKEN.*=" --include="*.py" | grep -v "os.getenv"

# Проверить .gitignore
cat .gitignore | grep -E "\.env|\.db|\.sqlite"

# Проверить валидацию config.py
python3 -c "import config" 2>&1
# Должно выдать ошибку, если переменные не заданы
```

